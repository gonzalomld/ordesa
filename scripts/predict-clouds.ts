// scripts/predict-clouds.ts — N2: RETIRED (el layout 4c/main+far ya no existe).
// El layout N2 (24 grupos, rechazos contados) se audita en prod vía __cloudBand
// (base, techo, camYmax, aceptados/rechazados y por qué) + __cloudCoverPx.
// Este stub falla alto para que ningún paso obsoleto reviva en silencio.
throw new Error(
  "predict-clouds.ts retired (N2): audit __cloudBand + __cloudCoverPx in prod",
);
